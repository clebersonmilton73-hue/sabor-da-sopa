rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isAdmin() {
      return request.auth != null && (
        request.auth.token.email == "clebersonmilton73@gmail.com" ||
        request.auth.token.email == "sabordasopas@gmail.com"
      );
    }

    // NOVO PADRÃO do sistema
    match /produtos/{id} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    match /pedidos/{id} {
      allow create: if true;
      allow read, update, delete, list: if isAdmin();
    }

    // (Opcional) manter menuItems privado para migração / admin
    match /menuItems/{id} {
      allow read: if isAdmin();
      allow create, update, delete: if isAdmin();
      allow list: if isAdmin();
    }

    match /admins/{id} {
      allow read, write: if isAdmin();
    }
  }
}
